#include "main.h"

/**
 * _abs - check the function code
 *
 * @i: integer variable to char
 *
 * Return: value.
 */

int _abs(int i)
{
	if (i < 0)
	{
		i = i * -1;
		return (i);
	}
	else 
	{
		return (i);
	}
}
