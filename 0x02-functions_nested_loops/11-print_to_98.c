#include <stdio.h>

/**
 * print_to_98- check the function code
 *
 * @n: parameter variable
 *
 * Return: Always 0.
 */
void print_to_98(int n)
{
	while (n <= 98))
		{
			n++;
			printf("%d, ", n);
		}
	while (n >= 97)
	{
		n--;
		printf("%d, ", n);
	}
	
	printf("%d\n", n);
}
